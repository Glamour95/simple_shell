#ifndef HEADER_H
#define HEADER_H

#include <stdio.h>
#include <stdlib.h>
#include <stdarg.h>
#include <ctype.h>

#int interactive(info_t *info);
#int is_delim(char c, char *delim);
#int _isalpha(int c);
#int _atoi(char *s);

#endif
